import { Button } from "@mui/material";
import React from "react";

const Users = () => {
  return (
    <Button variant="contained">
      Cancel Contract
    </Button>
  );
};

export default Users;
